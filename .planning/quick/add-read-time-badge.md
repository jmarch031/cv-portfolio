# Quick Task: Add Read Time Badge to Articles

## Goal
Add a small badge indicating the reading time for each article in the "Écrits & Réflexions" section.

## Plan
1. Modify `src/styles/components.css`:
   - Add `.read-time-badge` style (small, accent color, subtle border/background).
2. Modify `public/locales/fr.json` and `public/locales/en.json`:
   - Add `pub_1_read_time`, `pub_2_read_time`, etc.
3. Modify `index.html`:
   - Add the HTML structure for the badge in each article card.
4. Verify:
   - Run `npm run build`.
   - Manual check.

## Execution
- [ ] Update src/styles/components.css
- [ ] Update locale JSONs
- [ ] Update index.html
- [ ] Build and verify
