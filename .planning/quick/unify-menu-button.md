# Quick Task: Unify Menu Button Style

## Goal
Make the menu toggle button look identical to the language and theme buttons (circular icon-button style).

## Plan
1. Modify `index.html`:
   - Change `#menu-toggle` classes to `nav-hover magnetic-btn icon-btn`.
   - Remove inline `style` attribute from `#menu-toggle`.
2. Verify:
   - Run `npm run build` to ensure no regressions.
   - Manual check of the UI.

## Execution
- [x] Update index.html
- [x] Build and verify
