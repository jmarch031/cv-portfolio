# Quick Task: Add Recommendations to Menu

## Context
The "Recommendations" section was recently added but is not yet accessible via the main navigation menu.

## Strategy
1. **Update Translations:** Add `menu_rec` key to `public/locales/fr.json` and `public/locales/en.json`.
2. **Update HTML:** Add a new `<li>` entry to the `.menu-list` in `index.html` pointing to `#recommendations`.
3. **Verify:** Ensure the link works and is correctly translated.

## Tasks
1. [x] Add `menu_rec` to `fr.json` and `en.json`.
2. [x] Add recommendations link to `index.html` menu.
3. [x] Update `STATE.md`.
